import 'package:flutter/material.dart';

class searchinvestor extends StatelessWidget {
  static const routname = "searchinvestor";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
    );
  }
}
