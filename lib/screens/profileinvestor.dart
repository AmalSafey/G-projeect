import 'package:flutter/material.dart';

class profileinvestor extends StatelessWidget {
  static const routname = "profileinvestor";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.yellow,
    );
  }
}
