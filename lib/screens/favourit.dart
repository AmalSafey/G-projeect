import 'package:flutter/material.dart';

class favourit extends StatelessWidget {
  static String routname = "favourit";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.yellow,
    );
  }
}
